{
  "name": "@behuman/sdk",
  "version": "0.0.0",
  "private": true,
  "description": "SDK cliente de beHuman — prover ZK + orquestación de tx Stellar",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@behuman/shared": "*",
    "@stellar/stellar-sdk": "^13.0.0",
    "snarkjs": "^0.7.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
